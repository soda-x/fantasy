{
  "private": true,
  "scripts": {

  },
  "lint-staged": {
    "*.js": [
      "prettier --trailing-comma es5 --single-quote --write",
      "git add"
    ]
  },
  "devDependencies": {
    "eslint": "^4.18.1",
    "husky": "^0.14.3",
    "lerna": "^2.2.0",
    "lerna-changelog": "^0.7.0",
    "lint-staged": "^7.0.0",
    "prettier": "^1.10.2"
  }
}
